
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
    <link rel="stylesheet" href="./stylesheets/createproduct.css">
</head>
<body>
    <div class="headline">
        <h2>Add Your Product for biding</h2>
    </div>
    <div class="center">
        <div class="pform">
            <h2>create product</h2>
            <form method="post" action="/create">  
                <input id="pname" type="text" placeholder="Product Name" name="pname">
                <input id="price" type="number" placeholder="Price" name="price">
                <input type="text" placeholder="Product image Url" name="image">
                <textarea placeholder="Product Description " name="description" id="desc" cols="49" rows="10"></textarea>
                <input type="date" id="inputdate" name="inputdate">
                <!-- <input id="inputdate" type="datetime-local" name="inputdate"> -->
                <!-- <input type="time" name="inputtime" onchange="onTimeChange()" id="timeInput" /> -->
                <input type="time" name="inputtime">
                <input id="submit" type="submit" value="Submit">
            </form>
        </div>
    </div>

</body>
    <script>
//         // TO DISABLE PREVIOUS DATES
    $(function(){
        var dtToday = new Date();
     
        var month = dtToday.getMonth() + 1;
        var day = dtToday.getDate();
        var year = dtToday.getFullYear();
        if(month < 10)
            month = '0' + month.toString();
        if(day < 10)
         day = '0' + day.toString();
        var maxDate = year + '-' + month + '-' + day;
        $('#inputdate').attr('min', maxDate);
    });
    // TO DISABLE PAST DATES
    $(function(){
    var dtToday = new Date();

    var month = dtToday.getMonth() + 1;
    var day = dtToday.getDate();
    var year = dtToday.getFullYear();
    day = day+5;

    if(month < 10)
        month = '0' + month.toString();
    if(day < 10)
        day = '0' + day.toString();

    var maxDate = year + '-' + month + '-' + day;    
    // console.log(maxDate);
    $('#inputdate').attr('max', maxDate);
});

//     // Time 
//     var inputEle = document.getElementById('timeInput');


// function onTimeChange() {
//   var timeSplit = inputEle.value.split(':'),
//     hours,
//     minutes,
//     meridian;
//   hours = timeSplit[0];
//   minutes = timeSplit[1];
//   if (hours > 12) {
//     meridian = 'PM';
//     hours -= 12;
//   } else if (hours < 12) {
//     meridian = 'AM';
//     if (hours == 0) {
//       hours = 12;
//     }
//   } else {
//     meridian = 'PM';
//   }
//   console.log(hours+minutes+meridian)
//   alert(hours + ':' + minutes + ' ' + meridian);
// }
    </script>
</html>